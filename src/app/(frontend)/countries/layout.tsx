import type { ReactNode } from 'react';

/**
 * Route-group layout for country hub URLs; page chrome comes from ContentPageShell (MainLayout).
 */
export default function CountriesLayout({ children }: { children: ReactNode }) {
  return children;
}
