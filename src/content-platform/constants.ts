/** ISR revalidation interval in seconds (Tina / content updates). */
export const CONTENT_REVALIDATE_SECONDS = 60;

/** Root for MDX and JSON content (repo root). */
export const CONTENT_ROOT = 'content';
