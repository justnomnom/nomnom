'use client';

import { PostDetailsSkeleton } from '../post-skeleton';

// ----------------------------------------------------------------------

/** Route `loading.js` for blog post detail — mirrors in-view `PostDetailsSkeleton`. */
export default function PostDetailsRouteLoading() {
  return <PostDetailsSkeleton />;
}
