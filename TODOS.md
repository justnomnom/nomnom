# NomNom — Design TODOs

Generated by `/plan-design-review` audit on 2026-05-05.

---

## High Priority

### Add author identity to homepage testimonial
**File:** `src/sections/home/home-testimonial.js`

**What:** Add an avatar image, display name, and @handle below the testimonial quote. The component currently renders only the quote and a kicker heading — no person is identified.

**Why:** An anonymous testimonial on a "trust the person" platform is self-defeating. The brand promise is "picks from people you trust" — having an unattributed quote contradicts that message at the top of the funnel.

**Pros:** ~15 lines of markup. Immediate trust signal for homepage visitors.

**Cons:** Can't ship a placeholder — requires getting a real user's permission to be featured.

**Context:** `home-testimonial.js` renders the quote from i18n keys (`pages.home.testimonial.*`). The avatar can be sourced the same way as `HERO_CIRCLE_AVATARS` in `home-hero.js` (SVG or hosted image). The @handle should link to `paths.dashboard.userPublic(handle)`.

**Implementation sketch:**
```jsx
// Below the blockquote, add:
<Stack direction="row" alignItems="center" spacing={1.5} sx={{ mt: 3 }}>
  <Avatar src={TESTIMONIAL_AVATAR} sx={{ width: 44, height: 44 }} />
  <Box>
    <Typography variant="subtitle2" sx={{ fontWeight: 700 }}>{TESTIMONIAL_NAME}</Typography>
    <Typography variant="caption" sx={{ color: 'text.secondary' }}>@{TESTIMONIAL_HANDLE}</Typography>
  </Box>
</Stack>
```

**Depends on:** Permission from a real user to feature them on the homepage.

---

## Low Priority

### Add narrative beat between Features and Testimonial on home below-fold
**File:** `src/sections/home/home-below-fold.js`

**What:** Insert a "how it works" / product-screenshot section between `HomeLandingFeatures` and `HomeTestimonial` to disrupt the generic SaaS section rhythm (features → quote → faq → ad).

**Why:** Identified in /design-review 2026-05-20. The current section stack reads as cookie-cutter marketing. A visual product beat (real screenshot of the map/discover UI, or 3-step "how it works" with imagery) would anchor the below-fold in the actual product instead of generic copy.

**Implementation:** New `HomeProductBeat` (or similar) section component. Needs real product screenshots — not generated/placeholder. Requires design + content collaboration.

**Depends on:** Real product screenshot assets.

---

## Done

- **Align dashboard microcopy with the "spots" vocabulary (§11)** — Done by /design-review on main, 2026-07-03 (user-approved). 26 strings per locale switched ("restaurants"→"spots" / "restaurantes"→"sítios"): gmaps import, freemium gate/pricing, saved tab + empty states, search/typeahead, detail not-found errors, public roulette states, help explainers. Kept: taglines, SEO meta/FAQ, nav links, admin tooling, entity labels, `{{restaurant}}` placeholders.
- **Replace hand-rolled 44px touch targets with `touchTargetSx`** — Done by /design-review on main, 2026-07-03. 6 files now use `touchTargetSx` / `TOUCH_TARGET_SIZE`; no raw `44`s left outside the theme.

- **Add `aria-label` to bottom navigation landmark** — Fixed by /design-review on main, 2026-06-10. Localized `navigation.main_navigation_aria` (en/pt) on the `Paper component="nav"` in `nav-bottom.js`.
- **Audit centered text across onboarding-wizard and settings-subscribers** — Done by /design-review on main, 2026-06-10. One real offender: the onboarding error alert (multi-line copy beside the severity icon) is now left-aligned. The wizard's location hint and celebration overlay, and all settings-subscribers instances (stat tiles, empty states), are hero/empty-state centering and were kept.
- **Align list-row gallery radius with RADIUS tokens** — Fixed by /design-review on main, 2026-06-10. `12px` → `RADIUS.base` (16px) in `map-spot-sheet-inner.js`; verified on the public list view and the map spot sheet.
- ~~Migrate settings-subscribers.js to TypeScript~~ — Removed 2026-06-10 (depends on a broader TS migration strategy that isn't planned; tracking it here added noise).
